Code to get list of prime numbers. The user input has to be greater than 100 for the code to execute

DROP PROCEDURE IF EXISTS P_PrimeNum;
Delimiter $$
CREATE PROCEDURE P_PrimeNum(n int)
sp:BEGIN
		DECLARE i,j,flag int;
		DROP TABLE IF EXISTS T_PrimeNum1;
        
        -- Exit when the input n is less than 100
	  IF n < 100 then
	  select 'GIVE HIGHER VALUE' Message;
	  leave sp;
	  end if;
	
	  CREATE temporary table T_PrimeNum1(Prime_Num int);
	  BEGIN
		  SET j =2;
				
		  WHILE(j<=n) DO   -- loop to start with 2 and to compare with the imput value 
		  SET flag = 0;
		  SET i = 2;

        WHILE(i<=j) DO  
				  if (j % i = 0) then 
				  set flag = flag+1;    
				  End if;
				  SET i=i+1;  
			  END while;

        IF(flag = 1) THEN
        INSERT INTO T_PrimeNum1 values(j);
        END IF;
        SET  j=j+1;

		  END WHILE;
	  END;
			
	  select * from T_PrimeNum1;
			
END$$
 
 call P_PrimeNum(200);
